# Copy this file to .env.local and fill in the Lambda Function URL.
# Get the URL from:
#   AWS Console → Lambda → vulnerability-agent → Configuration → Function URL
# OR from Terraform output:
#   cd infra && terraform output agent_function_url

VITE_AGENT_URL=https://<your-function-url-id>.lambda-url.us-east-1.on.aws/
