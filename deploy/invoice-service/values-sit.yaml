# ── Invoice Service — SIT Environment ─────────────────────────────────────────

compute:
  cpu:           256
  memory:        512
  desired_count: 1

env:
  ENV:       sit
  LOG_LEVEL: info
  REDIS_URL: "redis://localhost:6379"

env_secrets:
  DATABASE_URL:
    ssm_path: /orderflow/sit/invoice-service/db-url
